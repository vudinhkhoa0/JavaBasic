package STRING;

public class newKnowledge_String {
    // (**) Cú pháp: String name_String; 
    /*
        String s = sc.nextLine();
            s.length();
            Xử lý trôi lệnh: dùng sc.nextLine() ngay phía trên
            charAt(index): Lấy ký tự tại vị trí index;
            Muốn for(char x : ...), thì phải ... = s.toCharArray()
            s.toUpperCase(): chuyển toàn bộ sang in hoa 
            s.toLowerCase(): Chuyển toàn bộ sang in thường
            s1.concat(s2): Nối s1 + s2, (có thể dùng dấu cộng)
            s.compareTo(s2): So sánh, 0: bằng, -1: xâu hiện tại nhỏ hơn xâu cần ss; 1: ngược lại -1
            s.compareToIgnoreCase(s2): So sánh bỏ qua in hoa in thường
            s.equals(s2): nếu bằng trả về true
            s.equalsToIgnoreCase(s2): bằng (Bỏ qua in hoa in thường) trả về true    
            s.substring(index): Lấy chuỗi con từ vị trí thứ index
            s.subSequence(index_start, index_end): lấy chuỗi con từ vị trí index_start đến index_end;
            Các hàm kiểm tra ký tự (dùng lớp Character): 
                isDigit(char c): Kiểm tra chữ số 
                isLowerCase(char c): Kiểm tra chữ in thường 
                isUpperCase(char c): Kiểm tra chữ in hoa
                isAlphabetic(char c): Kiểm tra chữ cái
            String[] arr = s.split("\\s+"): Tách từng từ, Không nên truyền ' ': vì rất dễ tách bằng 1 dấu cách 
            (**) StringToKenizer    
                StringToKenizer st = new StringToKenizer(s);
                while (st.hasMoreTokens) {
                    System.out.println(st.nextToken);
                }
            s.contains(s'): Kiểm tra sự tồn tại của sâu con s';
            
    */
    // (***) String trong java không hỗ chợ thay đổi ký tự trong xâu
}
